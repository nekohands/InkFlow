# ADR 0001: 使用 Modular Monolith + 独立 Worker

- 状态：Accepted
- 日期：2026-08-20

## 决策

业务模块首先作为同一代码库中的明确边界实现；HTTP API、采集 Worker 与调度器采用独立进程。暂不拆分细粒度微服务。

## 原因

项目早期的主要风险来自业务模型、来源适配和内容管线，而不是服务规模。模块化单体可以降低部署与分布式事务成本，同时保留未来拆分边界。
